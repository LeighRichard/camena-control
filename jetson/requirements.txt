# 相机位置控制系统 - Python 依赖
# 项目仅支持 Python 3.6（TensorRT 要求）
# 仅支持 Jetson 平台

# 深度相机驱动（二选一或都安装以支持多种相机）
pyrealsense2>=2.50.0    # Intel RealSense 系列
pyorbbecsdk>=1.3.0      # 奥比中光系列

# 计算机视觉
numpy>=1.19.0,<1.24.0   # Python 3.6 兼容版本
opencv-python>=4.5.0

# 深度学习（目标检测）
# 注意: PyTorch 在 Jetson Nano 上需要特殊安装
# 请参考: https://forums.developer.nvidia.com/t/pytorch-for-jetson/72048
# torch>=1.10.0
# torchvision>=0.11.0

# TensorRT（Jetson Nano 已预装，无需安装）
# tensorrt>=8.0.0  # Jetson Nano 自带

# Web 服务
flask>=2.0.0
flask-cors>=3.0.10
flask-socketio>=5.1.0
python-socketio>=5.4.0

# 串口通信
pyserial>=3.5

# 配置文件
pyyaml>=5.4.0
python-dotenv>=0.19.0

# 数据处理
pandas>=1.1.0,<2.0.0    # Python 3.6 兼容版本
pillow>=8.0.0

# 测试
pytest>=6.0.0,<8.0.0    # Python 3.6 兼容版本
pytest-asyncio>=0.15.0
hypothesis>=6.0.0       # Property-Based Testing

# 日志和监控
psutil>=5.7.0           # 系统监控
requests>=2.25.0        # HTTP 请求

# 可选依赖（根据需要安装）
# face-recognition>=1.3.0  # 人脸识别（需要 dlib）
# insightface>=0.6.0       # InsightFace 人脸识别
